<Page x:Class="CodeTracker.Views.ProjectsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:CodeTracker.Views"
      xmlns:helpers="clr-namespace:CodeTracker.Helpers"
      xmlns:vm="clr-namespace:CodeTracker.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="900"
      Title="Projects">
    <Page.DataContext>
        <vm:ProjectsViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock Text="Projects" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

            <ListView ItemsSource="{Binding Projects}" SelectionMode="Single">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="250">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Description" Width="300">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Description}" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Total Time" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FormattedTotalTime}" FontSize="14" FontWeight="Bold" Foreground="#2196F3"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Actions" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Sessions"
                                               Command="{Binding DataContext.ViewProjectSessionsCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                               CommandParameter="{Binding}"
                                               Width="70" Height="30" Margin="2" FontSize="11"/>
                                        <Button Content="Delete"
                                               Command="{Binding DataContext.DeleteProjectCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                               CommandParameter="{Binding}"
                                               Width="60" Height="30" Margin="2" FontSize="11" Background="#F44336" Foreground="White"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>

        <Border Grid.Column="1" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Background="White"
               Padding="20" Margin="20,0,0,0" Width="400"
               Visibility="{Binding ShowProjectSessions, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Sessions for " FontSize="20" FontWeight="Bold"/>
                    <TextBlock Text="{Binding SelectedProject.Name}" FontSize="20" FontWeight="Bold" Foreground="#2196F3"/>
                    <Button Content="✕" Command="{Binding CloseSessionsViewCommand}"
                           Width="30" Height="30" Margin="10,0,0,0" FontWeight="Bold"
                           HorizontalAlignment="Right"/>
                </StackPanel>

                <TextBlock Text="{Binding SelectedProject.Description}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                <TextBlock FontSize="14" Margin="0,0,0,20">
                    <Run Text="Total Time: "/>
                    <Run Text="{Binding SelectedProject.FormattedTotalTime}" FontWeight="Bold" Foreground="#2196F3"/>
                </TextBlock>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding ProjectSessions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,10" Background="#F5F5F5">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                        <TextBlock FontSize="12" Foreground="#666">
                                            <Run Text="Duration: "/>
                                            <Run Text="{Binding FormattedDuration}" FontWeight="Bold"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding LanguagesString}" FontSize="11" Foreground="#888" Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding TimeStarted, StringFormat='dd/MM/yyyy HH:mm'}" FontSize="11" Foreground="#888"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </Border>
    </Grid>
</Page>
